services:
    postgres:
        image: 'postgres:17-alpine'
        container_name: aurum-db
        environment:
            - 'POSTGRES_DB=aurum'
            - 'POSTGRES_USER=user'
            - 'POSTGRES_PASSWORD=password'
        ports:
            - '5432:5432'
        volumes:
            - ./postgres_data:/var/lib/postgresql/data
